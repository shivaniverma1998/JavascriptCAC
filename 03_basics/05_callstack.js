//How does javascript execute code + call stack

//Concept - Javascript Execution Context - how javascript  run a file 
// there are two phases in which files executes 1. Global EC  {}   for example  -->this(iski value window object)
// 2. Functional Execution Context

// {} this is memory creation phase , (2) execution phase


// lets take piece of code to understand how javascript code Worker

//refer image.png : phase (1)  global esecution(this)---> phase(2) memory phase  value define (e.g val1 -> undefined) --->  phase(3) -> Execution phase (e.g val1 <- 10)
 

//CALL STACK  : REFER image (callstack.png & callstackfn.png)
